/* window.onload = function(){
    var obj = {
        name: "fred",
        major: "music",
        calculateFunction : function(a, b){
            if(a == b){
                return a*a + b*b;
            }
            else if(a > b){
                return b;
            }else
                return a;
        }
    }

    document.getElementById("result").innerHTML = obj.name;
} */